<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - ElectroStore</title>
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- IMPORTANT: This must match your public directory -->
    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        /* Fallback inline styles in case external CSS fails */
        .fallback-message {
            background: #fef3c7;
            border: 1px solid #f59e0b;
            color: #92400e;
            padding: 1rem;
            margin: 1rem;
            border-radius: 0.375rem;
        }
    </style>
</head>
<body>
    <div class="fallback-message" style="display: none;" id="css-fallback">
        ⚠️ CSS is not loading properly. Please check that the file exists at /css/style.css
    </div>

    <header class="header">
        <div class="container">
            <nav class="navbar">
                <a href="/" class="logo">
                    <i class="fas fa-store"></i>
                    <span>ElectroStore</span>
                </a>
                <ul class="nav-links">
                    <li><a href="/"><i class="fas fa-home"></i> Dashboard</a></li>
                    <li><a href="/categories"><i class="fas fa-list"></i> Categories</a></li>
                    <li><a href="/items"><i class="fas fa-box"></i> Items</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main class="container">
        <%- body %>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2026 ElectroStore Inventory System</p>
        </div>
    </footer>

    <button class="theme-toggle" id="themeToggle">
        <i class="fas fa-moon"></i>
    </button>

    <script>
        // Check if CSS loaded
        window.onload = function() {
            const bodyStyles = window.getComputedStyle(document.body);
            const bgColor = bodyStyles.backgroundColor;
            
            if (bgColor === 'rgba(0, 0, 0, 0)' || bgColor === 'transparent') {
                document.getElementById('css-fallback').style.display = 'block';
                console.error('❌ CSS failed to load!');
            } else {
                console.log('✅ CSS loaded successfully!');
            }
        };
    </script>
    
    <script src="/js/main.js"></script>
</body>
</html>
